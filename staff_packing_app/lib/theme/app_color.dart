// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

/// --- Brand Palette (Purple) ---
class AppColors {
  // Primary Brand Color
  static const Color navyblue = Color(0xFF011D3E);

  // Darker shade for buttons / shadows
  static const Color navyblueDark = Color(0xFF001528);

  // Light tint for backgrounds / accents
  static const Color navyblueLight = Color(0xFFE3E9F3);

  // Main text
  static const Color textDark = Color(0xFF1C1C1E);

  // Muted text
  static const Color textMuted = Color(0xFF6C6C70);

  // Soft background (soft off-white with a hint of violet)
  static const Color bg = Color(0xFFF4F6FA);
}
