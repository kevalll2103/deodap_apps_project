import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xff0b90a1);  // Teal Blue
  static const Color primaryDark = Color(0xff097a8c);
  static const Color primaryLight = Color(0xff56c8d8);

  // Secondary Colors
  static const Color secondary = Color(0xff2196f3); // Light Blue
  static const Color secondaryDark = Color(0xff1976d2);
  static const Color secondaryLight = Color(0xff64b5f6);

  // Accent Colors
  static const Color accent = Color(0xff4caf50);   // Green
  static const Color accentDark = Color(0xff388e3c);
  static const Color accentLight = Color(0xff81c784);

  // Error/Warning Colors
  static const Color error = Color(0xffe53935);    // Red
  static const Color warning = Color(0xffffa000);  // Amber
  static const Color success = Color(0xff4caf50);  // Green

  // Background Colors
  static const Color background = Color(0xfff5f5f5); // Light Grey
  static const Color surface = Color(0xffffffff);   // White
  static const Color cardBackground = Color(0xffffffff);

  // Text Colors
  static const Color textPrimary = Color(0xff212121);
  static const Color textSecondary = Color(0xff757575);
  static const Color textDisabled = Color(0xffbdbdbd);

  // Border Colors
  static const Color border = Color(0xffe0e0e0);
  static const Color divider = Color(0xffe0e0e0);

  // Loading/Progress Colors
  static const Color loading = Color(0xff0b90a1);
  static const Color progress = Color(0xff0b90a1);

  // Disabled States
  static const Color disabled = Color(0xffbdbdbd);
  static const Color disabledBackground = Color(0xffe0e0e0);

  // Special Colors
  static const Color recycleBin = Color(0xff0b90a1);
  static const Color noInternet = Color(0xff0b90a1);
  static const Color delete = Color(0xffe53935);
  static const Color recover = Color(0xff4caf50);
}
